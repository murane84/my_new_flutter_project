fastapi
uvicorn
websockets
# Pin to the 2.0 line: an unpinned SQLAlchemy rebuild changed which Postgres
# driver a bare "postgresql://" URL resolves to (reaching for psycopg v3), which
# broke the deploy. Staying on 2.0.x keeps the driver + declarative API stable.
sqlalchemy>=2.0,<2.1
# psycopg2-binary ships a precompiled wheel (no libpq/gcc build step on the
# host) — reliable on Railway/nixpacks. config.py forces the psycopg2 dialect.
psycopg2-binary
python-dotenv
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]
alembic
pydantic[email]
python-multipart
# Firebase Cloud Messaging (HTTP v1) push: mint OAuth2 tokens from a service
# account (google-auth) and POST to FCM (requests). Both optional at runtime —
# if Firebase env vars are unset, push is skipped and the app still works.
google-auth
requests
# Two-factor / password recovery: TOTP (Google Authenticator) code generation
# and verification.
pyotp
